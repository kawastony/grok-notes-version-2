# Simultaneous progress: identity, propagation, unification

Tony Kawas / 19 September 2026.

Goal: advance all three tracks **in parallel** without inflating unification.

Scorecard baseline (external assessment): Identity ~85%, Propagation ~78%, Unification ~38%.

---

## 0. Discipline

| Track | Allowed move | Forbidden move |
|---|---|---|
| Identity | Larger residual-clean scans, class boundaries, stability | Treating taxonomy as gravity |
| Propagation | Mediator tests, out-of-sample flows, constitutive calibration | Claiming continuum limit done |
| Unification | Architectural bridge + one falsifiable macro test design | Lattice = Einstein / a_T derived |

Locked TAFA constitution (notes-by-grok) stays authoritative for SPARC / clock / η. This repo stays the micro / lattice lab.

---

## 1. Identity track — next derivations & tests

### 1.1 What is already earned
- 8-component Wilson–Callias embedding
- Residual-clean soft sector on pair defects
- Midpoint / hollowing / anisotropy diagnostics
- Soft-mode pair identity (hybridization → molecular regime)

### 1.2 Tests to raise Identity toward ~90%

| Test | Protocol | Success criterion |
|---|---|---|
| **I-A** Larger residual-clean grid | L≥10, denser (v₁,v₂) or (m₀,sep) | Soft sector stable; taxonomy unchanged |
| **I-B** Class boundary | ROC or silhouette on residual-clean labels | Clear separation baseline vs polarized |
| **I-C** Parameter-direction stability | ±15% and orthogonal probes | Same identity features rank-stable |

### 1.3 Short derivation (identity object)

Define the **identity vector** on residual-clean soft support:
\[
\mathbf{I}
=
\bigl(R_{\mathrm{mid}},\,c,\,|D_1|,\,A-1,\,\chi_{\beta n}\bigr).
\]

**Claim (to be stress-tested):** soft-sector states are clustered in \(\mathbf{I}\)-space with residual-clean gap from bulk modes.

Not a theorem yet — a measurable definition that makes “identity” operational.

---

## 2. Propagation track — next derivations & tests

### 2.1 What is already earned
- Identity correlates with \(\Delta W_{\mathrm{mid}}\) / early Forman response
- Relative Forman curvature as mediator
- Candidate continuity law (anomaly-shaped):
\[
\partial_t\rho_\chi+\nabla\cdot\mathbf{J}_\chi
=
S_{\mathrm{top}}-\Gamma_f\rho_\chi,
\qquad
\mathbf{J}_\chi
=
-D_\chi\nabla\rho_\chi
+\kappa\rho_\chi\mathbf{B}_{\mathrm{eff}}
+\boldsymbol\xi_{\mathrm{cone}}.
\]

### 2.2 Tests to raise Propagation toward ~85%

| Test | Protocol | Success criterion |
|---|---|---|
| **P-A** Mediator ablation | Hold \(\mathbf{I}\), scramble Forman weights | \(\Delta W\) prediction collapses if curvature is causal mediator |
| **P-B** Out-of-sample flow rules | ≥3 Forman-type / Ricci-like discrete flows | Rank order of \(\Delta W_{\mathrm{mid}}\) preserved |
| **P-C** Separation scan | Vary sep at fixed residual-clean identity class | Hybridization timescale / \(D_\chi\) trend monotonic |

### 2.3 Short derivation (propagation object)

**Continuity skeleton** is standard (Callan–Harvey-type inflow).  
**Constitutive piece** remains ansatz:
\[
\mathbf{J}_\chi^{\mathrm{(drift)}}
=\kappa\rho_\chi\mathbf{B}_{\mathrm{eff}}.
\]

**Lattice-facing prediction:** soft anti-alignment (\(\chi_{\beta n}<0\)) flips the sign of early bridge-feeding relative to bulk-aligned texture under the same discrete “\(\mathbf{B}_{\mathrm{eff}}\)” proxy.

That is a **within-model** falsifier — does not require galaxies.

---

## 3. Unification track — careful, simultaneous progress

### 3.1 What “unification” is allowed to mean *now*

| Level | Meaning | Status |
|---|---|---|
| U0 | Within-model: identity → curvature → flow | **In progress** (strongest) |
| U1 | Same *template* at micro and residual-network scales | Architectural hypothesis |
| U2 | Continuum limit of lattice → effective metric / stiffness | **Not done** |
| U3 | Derive \(a_T\) or \(z_{\mathrm{act}}\) from lattice | **Not claimed** |

Raise unification by locking U0 and designing U1 tests — **not** by asserting U3.

### 3.2 Simultaneous bridge tests (do not claim success early)

| Test | What it does | Unification level |
|---|---|---|
| **U-A** Template identity | Write one constitutive form used on lattice *and* on residual \(\Sigma_{\mathrm{res}}\) | U1 design |
| **U-B** SPARC winding residual | Split residuals by Z/S if labels exist; same \(a_T\) freeze | Empirical U1 probe |
| **U-C** Continuum target list | Pre-register what a continuum limit must reproduce (gap, index, \(\Delta W\) sign) | Roadmap only |

### 3.3 Honest non-goals this cycle

- Einstein equations on the lattice graph  
- \(a_T\) from Forman flow  
- \(z\sim 0.3\) from soft-mode hybridization  

Those remain open theory tracks, not current deliverables.

---

## 4. Parallel work package (recommended order)

**Week structure (can overlap):**

1. **Identity:** densify residual-clean grid (I-A); freeze \(\mathbf{I}\) definition.  
2. **Propagation:** mediator ablation (P-A) on existing 3+ archetypes; write pass/fail.  
3. **Unification:** implement SPARC winding residual pipeline *if* labels available (U-B); otherwise complete U-A template note only.  
4. **Integration note:** one page “what moved on the scorecard” — no score inflation without new residual-clean n.

---

## 5. Scorecard update rule

Only change a track’s score when:

- new residual-clean data exist, or  
- a pre-registered falsifier was run and documented.

Narrative improvement without numbers does **not** move Identity or Propagation.  
Unification moves only when U0 is tighter *or* a U1 empirical probe is completed (even if null).

---

## 6. Compact simultaneous claim

- **Identity:** operationalize \(\mathbf{I}\); stress-test on denser residual-clean grids.  
- **Propagation:** treat Forman/relative curvature as mediator; ablate and out-of-sample.  
- **Unification:** strengthen within-model chain; design macro probes without claiming derivation of locked TAFA observables.

One line:

> Progress all three by making identity measurable, propagation falsifiable inside the lattice, and unification a template-plus-probe program — never a premature scale-jump to \(a_T\) or Einstein.
