# Executed package: identity · propagation · scale-up

Tony Kawas / 19 September 2026.

**Lineage:** builds on the 49 physics papers. Streamlines where cone + lattice + chirality replace heavier bookkeeping. Reality is a puzzle with moving pieces — this programme aims to track identity, influence, and scale structure without freezing the puzzle into one path’s constitution.

**Authority:** this repository. Microscopic anchors reused where forced:
\[
R_{\mathrm{cone}}=\sqrt6/\varphi,\qquad \kappa=R_{\mathrm{cone}}/c.
\]

---

# 1. Identity — locked operational definition

## 1.1 Identity vector (frozen for this programme)

On residual-clean soft support:
\[
\boxed{
\mathbf{I}
=
\bigl(
R_{\mathrm{mid}},\;
c,\;
|D_1|,\;
A-1,\;
\chi_{\beta n},\;
\mathrm{IPR},\;
w_{\mathrm{core}}
\bigr)
}
\]

Shape-layer proxies already measured (L=10 residual-validated):

| case | aniso \(A\) | quad \(c\) | \(D_1\) | mid character |
|------|-------------|-----------|--------|----------------|
| baseline | 1.56 | −0.0074 | −0.18 | mid peak, balanced |
| +15% | 1.47 | −0.0104 | +0.03 | sharper peak, balanced |
| −15% | 1.86 | +0.0043 | +0.29 | mid dip, polarized |

**Class contrast (locked reading):**
- **Collective / bridge-fed:** \(c<0\), low \(|D_1|\), moderate \(A\)
- **Polarized / hollow:** \(c>0\), high \(|D_1|\), higher \(A\)

Major axis locked to pair axis (align = 1) in all three states — geometric identity of the pair object.

## 1.2 What still raises Identity

- Larger residual-clean \(n\) (L, sep, \(m_0\))
- Continuum index \(=N_{\mathrm{def}}\)
- Decoupling at sep \(\gg\) core

Until then: **Identity is operational and class-separated on current residual-clean sample.**

---

# 2. Propagation — falsifiers written against existing data

## 2.1 Rank facts already in hand (Stage 1–2)

From residual-validated trio:

| quantity | rank (favorable → unfavorable for bridge) |
|----------|-------------------------------------------|
| mid peak (\(c<0\) first) | +15% ≻ base ≻ −15% |
| balance (low \(|D_1|\) first) | +15% ≻ base ≻ −15% |
| mid Forman (less negative first) | +15% ≈ −15% ≻ base (weak separator) |

Stage-1 shape separates collective vs polarized **strongly**.  
Mid Forman moves **same direction** for ±15% — weak as sole mediator.

## 2.2 Pre-registered falsifiers (run on next residual-clean expand)

**P1 — Sign / class flip**  
If a state with opposite soft alignment (\(\chi_{\beta n}\) sign flip) is residual-clean, early bridge-feeding rank must reverse relative to the polarized hollow class under the same discrete drive proxy.

**P2 — Mediator ablation**  
At fixed \(\mathbf{I}\) (or fixed Stage-1 shape class), scramble Forman edge weights.  
- If \(\Delta W_{\mathrm{mid}}\) rank **survives**, Forman is not the causal mediator.  
- If rank **collapses**, relative curvature mediation is supported.

**Current hint:** Stage-1 already predicts class without Forman; P2 must show whether Forman adds independent predictive power or only correlates.

**P3 — Separation / molecular trend**  
At fixed identity class, increasing sep must increase hybridization timescale (splitting \(\Delta\lambda\downarrow\), core localization \(\uparrow\)) until decoupling.

## 2.3 Constitutive law (streamlined)

\[
\partial_t\rho_\chi+\nabla\cdot\mathbf{J}_\chi=S_{\mathrm{top}}-\Gamma_f\rho_\chi,
\qquad
\mathbf{J}_\chi=-D_\chi\nabla\rho_\chi+\kappa\rho_\chi\mathbf{B}_{\mathrm{eff}}+\boldsymbol\xi_{\mathrm{cone}}.
\]

- Continuity: anomaly-family (papers + Callan–Harvey line).  
- \(\kappa=R_{\mathrm{cone}}/c\): cone micro.  
- Drift: ansatz until continuum limit fixes it.

---

# 3. Continuum targets (required before serious scale-up)

Pre-registered. Lattice is successful when it approaches these — not when narrative is smoothed.

| ID | Target |
|----|--------|
| C1 | Index \((D)\to N_{\mathrm{def}}\) (Callias / JR) |
| C2 | Bulk gap \(\Delta_{\mathrm{bulk}}\to m_0\) (already matched at small L) |
| C3 | Soft localization length scaling with \(m_0\), sep |
| C4 | Sign of bridge response under \(\chi_{\beta n}\) flip (links to P1) |
| C5 | Local effective action containing \(\mathcal{L}_{\mathrm{chiral}}\) with \(\kappa=R_{\mathrm{cone}}/c\) |

**Streamlining vs 49 papers:** continuum targets replace scattershot UV dictionary goals with a short checklist. Cone numbers stay; heavy 10D bookkeeping for \(R\) / slope accounting stays retired.

---

# 4. Residual handedness observable (U1 scale-up probe)

## 4.1 Construction (no new free parameters)

Microscopic:
\[
\mathcal{L}_{\mathrm{chiral}}
=
\frac{\kappa}{2}\,
\chi\,\bigl(\hat{\mathbf{c}}\cdot(\boldsymbol\varepsilon\times\partial_t\boldsymbol\varepsilon)\bigr).
\]

Residual promotion:
\[
\boldsymbol\varepsilon\to\boldsymbol\Sigma_{\mathrm{res}},\qquad
\hat{\mathbf{c}}\to\hat{\mathbf{n}}
\quad(\text{disk or residual-network normal}).
\]

## 4.2 Observable design (falsifiable)

**Name:** Handedness residual split.

1. Take a galaxy (or residual-field) sample with an independent winding / sense label \(Z\) vs \(S\) (or equivalent parity-odd tracer).  
2. Compute rotation-curve or residual-strain residuals under a **fixed** baseline acceleration rule (no retune).  
3. Split residual distribution by sense label.  
4. **Null hypothesis:** no difference between \(Z\) and \(S\) residual medians.  
5. **Programme hypothesis:** template chirality induces a measurable median shift or distribution shape difference correlated with sense.

**Pass / fail:**  
- Significant split in the predicted direction → U1 empirical support.  
- Null → template not yet coupled at observable level (not a failure of lattice identity).

This is how the puzzle piece “handedness” is tracked at galactic scale without claiming lattice = gravity.

## 4.3 What this does **not** claim

- Derivation of \(a_T\) from Forman flow  
- Continuum Einstein equations  
- Completion of all 49-paper UV sectors  

It claims a **clean test slot** for residual chirality inherited from cone \(\kappa\).

---

# 5. Scale-up map (how larger scales become possible)

| Step | Content | Puzzle piece tracked |
|------|---------|----------------------|
| Micro identity | \(\mathbf{I}\), soft sector, index targets | “What is the object?” |
| Propagation | Continuity + constitutive + P1–P3 | “Why does elsewhere respond?” |
| Residual template | Same law, coarse fields | “Same rule, larger grain” |
| Handedness split | Data probe | “Does the rule show up outside the lattice?” |
| Continuum C1–C5 | Effective action | “Can the lattice become a field theory?” |
| Later | Dynamics of that action → acceleration law | “Can floors emerge without targets?” |

**Streamlining:** one identity object, one continuity law, one \(\kappa\), one residual promotion, one empirical split — instead of many disconnected dictionary entries.

---

# 6. Scorecard rule (this programme)

| Track | Moves when |
|-------|------------|
| Identity | New residual-clean points or continuum index match |
| Propagation | P1–P3 run and documented |
| Unification | C-targets progress **or** residual handedness test executed (even null) |

No score inflation from prose alone.

---

# 7. Compact close

- **Identity locked** as measurable \(\mathbf{I}\) with collective vs polarized classes on current residual-clean trio.  
- **Propagation** has three pre-registered falsifiers and a constitutive skeleton using cone \(\kappa\).  
- **Scale-up** is a ladder: continuum targets → residual template → handedness residual observable → only later dynamical floors.  
- **49 papers:** streamlines bookkeeping through cone anchors and a short continuum checklist; does not erase the puzzle — it tracks moving pieces with fewer free labels.

One line:

> Same cone numbers, operational identity, falsifiable influence, residual handedness as the first real scale probe — a streamlined path across scales without pretending the continuum limit is already done.
