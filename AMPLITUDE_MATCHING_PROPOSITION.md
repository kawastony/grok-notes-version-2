# Proposition: amplitude matching R_cone ↔ A√(2κ₀)

## Statement

There exists a matching
\[
A\sqrt{2\kappa_0}\;\stackrel{?}{=}\;f(R_{\rm cone},\varphi,\Sigma_{\rm ref},\ldots)
\]
such that the Paper 24 normalization and the micro-cone amplitude R_cone=√6/φ describe **the same** O(1) geometric factor — not independent multipliers.

## Motivation

- Paper 24 fits A with κ₀=0.394 fixed; preferred form has no free Q.
- Micro program locks R_cone=√6/φ≈1.51387 from activation surface (φ′)²=6.
- Multiplying both double-counts cone geometry.

## Minimal testable form (proposed)

At a reference surface density Σ_ref (e.g. sample median),
\[
a_{\rm eff}(\Sigma_{\rm ref})=A\sqrt{2\kappa_0}\;\stackrel{?}{=}\;c\,R_{\rm cone}\,a_\ast
\]
for a constant c of geometric origin (ideally 1 or √2 or φ-related) and a_ast a fixed acceleration unit already in the theory.

**Pass:** derive c from cone matching without SPARC retuning, then check residual MAE does not degrade.
**Fail:** only c fitted to SPARC (then R_cone is cosmetic).

## Status

Proposition only. **Not derived** in this session. Canonical rule until closed: use A√(2κ₀) for SPARC phenomenology; treat R_cone as candidate identity for that amplitude, not an extra factor.
